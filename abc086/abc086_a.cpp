#include <bits/stdc++.h>

#define o(output) cout << output << endl

using namespace std;

int a,b,c,d;

int main(){
  cin >> a >> b;
  if((a*b)%2==1){
    o("Odd");
  }else{
    o("Even");
  }
}